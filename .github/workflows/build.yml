name: Android APK Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Kodu Indir
        uses: actions/checkout@v4

      - name: Node.js Kurulumu
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Java Kurulumu
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Kutuphaneleri Yukle
        run: npm install

      - name: Expo Prebuild (Android altyapisini hazirla)
        run: npx expo prebuild --platform android

      - name: APK Derleme (Gercek Uretim Surumu)
        run: |
          export NODE_OPTIONS="--max-old-space-size=2048"
          cd android
          ./gradlew assembleRelease -Dorg.gradle.jvmargs="-Xmx2048m" -Dorg.gradle.workers.max=1

      - name: APK'yi Indirilebilir Yap
        uses: actions/upload-artifact@v4
        with:
          name: SablonCizici-Hazir-APK
          path: android/app/build/outputs/apk/release/app-release.apk
